{% extends('MainBundle::base.html.twig') %}
{% block content %}
    <table class="table table-striped table-bordered">
    {% for row in adverts %}
        
            <tr>
                <td>{{row.title}}</td>
            </tr>    
        
    {% endfor %}
    </table>
{% endblock %}
